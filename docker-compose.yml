services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: runlogbook
      POSTGRES_PASSWORD: runlogbook
      POSTGRES_DB: runlogbook
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
